import React from "react";
import PropTypes from "prop-types";
import PageLayout from "../../Layout/PageLayout";
const Compare = ({ location }) => {
  return <PageLayout page="compare"></PageLayout>;
};

Compare.propTypes = {};

export default Compare;
